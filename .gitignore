# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Type checking
.mypy_cache/
.dmypy.json
dmypy.json

# Linting
.ruff_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment files
.env
.env.*

# Virtual environments
.venv/
venv/
env/

# Node.js
node_modules/

# Claude Code
.claude/**
# The PROJECT memory scope lives in-repo under .claude/ and MUST be tracked +
# pushed (the shared, cross-dev wiki memory). Re-include it via these exceptions
# (each parent dir re-included first, then the whole tree — order matters).
!.claude/project/
!.claude/project/memory/
!.claude/project/memory/**

# Test artifacts
coverage/
*.log

# Cache
.cache/
.pytest_cache/

# LLM Externalizer
llm_externalizer_output/

# TLDR
.tldr/

# Dev folders (NEVER PUBLISH - development artifacts only)
# Wildcard pattern catches all: docs_dev, scripts_dev, tests_dev, samples_dev,
# examples_dev, downloads_dev, libs_dev, builds_dev, etc.
*_dev/

# Rust
target/
Cargo.lock

# SERENA MCP state (local project metadata, not published)
.serena/

# git-cliff scratch — ephemeral release notes between bump and push
.git-cliff-release-notes.md

# Agent/skill reports — may contain private paths, tokens, internal notes.
# Never commit (per ~/.claude/rules/agent-reports-location.md).
/reports/
/reports_dev/

# Added by CPV standardize
.coverage
